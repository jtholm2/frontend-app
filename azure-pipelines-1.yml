trigger:
- main

pool:
  vmImage: 'Ubuntu-18.04'

variables:
  imageName: 'frontendapp'

steps:
- task: Docker@2
  displayName: Build an image
  inputs:
    repository: $(imageName)
    command: build
    Dockerfile: frontendapp/Dockerfile